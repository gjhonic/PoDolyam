APP_VERSION=0.2.0
# Только разработка; скопируйте в .env. Не использовать этот пароль на VPS.
POSTGRES_DB=podolyam
POSTGRES_USER=podolyam
POSTGRES_PASSWORD=podolyam_local_only
POSTGRES_PORT=5432
# Compose подставляет значения выше. Backend этапа 1 пока не обращается к БД.
# HTTP_ADDR для запуска Go вне Compose задаётся в окружении процесса.
